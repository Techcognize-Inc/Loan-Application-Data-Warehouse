name: 'loan_dw'
version: '1.0.0'
config-version: 2

profile: 'loan_dw'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  loan_dw:

    staging:
      +schema: staging
      +materialized: view

    warehouse:
      +schema: warehouse
      +materialized: table
